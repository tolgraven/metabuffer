This is a core feature to the plugin.
We need a mode (available if starting with a flag, and possible to enter when already running)
that also pulls in all other files in repo for filtering.
We show where a hit is from in a floating window to the right (this was partly implemented in the python plugin)

<CR> on a hit should open that file "for real" and jump to file and line.

