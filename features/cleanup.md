# Project cleanup

The plugin still has no user since it's in early development, so we do not need any backward compatibility, shimming, legacy stuff (like in `fnl/metabuffer/action.fnl`). Do a full pass of all the Fennel and identify things that can be safely cleaned up and removed.
Make a new branch for this.
