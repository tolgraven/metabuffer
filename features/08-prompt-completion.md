# Prompt completion

## Regular completion framework support
- We want the prompt input to be able to support the same completions (from cmp, etc) as at least the origin buffer from :Meta start (even in project mode).
- We also probably want our own completions as functionality grows, especially for `#commands` and flags.
